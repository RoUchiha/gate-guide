{
  "name": "gate-guide",
  "version": "1.0.0",
  "private": true,
  "description": "Spec-driven airport navigation PWA: track your flight and get closure-aware, accessible walking routes to your gate.",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build-static.js",
    "start": "node scripts/local-server.js",
    "test": "node --test tests/*.test.js",
    "check": "node scripts/spec-check.js",
    "icons": "node scripts/generate-icons.js",
    "verify": "npm run check && npm test && npm run build"
  }
}
